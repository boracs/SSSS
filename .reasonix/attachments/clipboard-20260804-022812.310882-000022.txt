Modo: Agent. Grok en Cursor. SOLO UI. Cumple EXACTAMENTE esto.

OBJETIVO
Rediseñar la vista del carrito CON productos para que combine con EmptyCartView
y el sistema S4 (slate + cyan + rose). Lógica de negocio INTACTA.
Resultado: simple, profesional, sin aspecto "Bootstrap 2019".

ARCHIVO ÚNICO
resources/js/Pages/Carrito.jsx

PRE-VUELO
Lee Carrito.jsx entero. Identifica: EmptyCartView (NO tocar), rama vacía
(`productos.length === 0`), rama llena, modal eliminar, toast.
NO reescribas EmptyCartView.

HECHOS DEL CÓDIGO (no contradecir)
- Vista vacía: shell `bg-slate-50` + `max-w-3xl` + H1 "Tu carrito".
  EmptyCartView: `rounded-2xl bg-white shadow-sm ring-1 ring-slate-200`.
- Vista llena hoy: `bg-gray-100`, `max-w-lg`, gray/red/violet, `key={index}`,
  modal con ⚠️, Eliminar lista en `bg-red-500`, total en `text-red-500`,
  CTA en `bg-violet-600`.
- Cada producto trae: id, nombre, precio, cantidad, subtotal, descuento.
  NO hay imagen.
- precio/subtotal/total suelen llegar como number desde Inertia; a veces pueden
  ser string. NUNCA asumas formato "15,00". El helper debe tolerar number y string.
- `iniciarPagoStripe`, `eliminarProducto`, `abrirModal`, `cerrarModal`,
  `useEffect(flash)`: NO cambies firmas ni cuerpos. Solo retoca el JSX que los llama.

PROHIBIDO
- Otros archivos, backend, PHP, Stripe, rutas, .env, tailwind.config.js
- Inventar imagen, talla, stock, SKU, controles +/- cantidad
- Borrar variables no usadas (paymentModalOpen, paymentIban, paymentBizumNumber,
  whatsappHelpUrl, abrirModalPagoPedido, etc.) — déjalas
- Reescribir EmptyCartView
- CTA violet/indigo/purple
- Total en `text-red-*`
- Botón Eliminar de la LISTA en `bg-red-500` sólido
- Emojis (ni nuevos ni los del modal/toast actuales)
- Newsletter, upsell, urgencia
- Cualquier mejora no listada en TAREAS

TAREAS (en este orden)

1) Helper `formatCartEur` (fuera del componente, mismo archivo)
```js
function formatCartEur(value) {
  const n = parseFloat(String(value ?? 0).replace(",", "."));
  return new Intl.NumberFormat("es-ES", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  }).format(Number.isFinite(n) ? n : 0);
}