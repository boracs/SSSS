# Coordinación — Archivo histórico
> Filas `HECHO` y entradas de actividad anteriores al 2026-08-10, movidas aquí para que `COORDINACION.md` no coma contexto (poda 2026-08-10, acordada Reasonix↔Cursor). Las tareas en curso y las últimas 10 quedan en `COORDINACION.md`.
## Estado actual (archivado)
| Fecha | Tarea | Quién | Estado | Archivos afectados |
| 2026-08-09 | Datáfono ticket: aviso disponibilidad alquiler (blocked ranges) | Cursor | HECHO | Fetch blocked_ranges; slots horas + aviso días; mismo motor que online |
| 2026-08-09 | Datáfono ticket: Crear clase walk-in si no hay programadas | Cursor | HECHO | `createWalkInLesson` + `POST …/datafono/lessons`; botón/form en modal; tests +2 |
| 2026-08-09 | Datáfono ticket: UX Alquiler (modo pastillas + packs chips) | Cursor | HECHO | `MostradorTicketModal.jsx` (Modo pastillas; packs chips con €; reset pack) |
| 2026-08-08 | Datáfono: walk-in clase en ticket (catálogo + reservar) | Cursor | HECHO | `catalogUpcomingLessons` + `resolveClasePayable`; UI filtros tipo/modalidad; guest+clase; tests 3/3 |
| 2026-08-08 | Datáfono: detalle móvil (líneas/servicios/fechas) | Cursor | HECHO | `listPayments` enriquece `ticket_lines` (label/fechas); acordeón + columna Servicios |
| 2026-08-08 | Datáfono: lista móvil sin scroll (acordeón) | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` (cards acordeón <md; tabla ≥md; `npm run build`) |
| 2026-08-08 | Datáfono: ticket multi-servicio (efectivo+TPV N líneas) | Cursor | HECHO | `mostrador_tickets`/`lines`, models, `MostradorTicketService`, `MostradorTicketLineDto`, controller store/assign, `MostradorTicketModal.jsx`, Index.jsx, tests 5/5 + Datafono 29/29 |
| 2026-08-08 | Segunda mano: soft-delete (desactivar) en vez de borrar | Cursor | HECHO | migración `deleted_at`, `SecondHandBoard` SoftDeletes, destroy→soft + restore, Index.jsx UI Desactivar/Reactivar + filtro Desactivadas |
| 2026-08-08 | Usuarios: quitar página «Seguimiento VIP» (salud/renovación, redundante y no alineada con el negocio) | Cursor | HECHO | GlobalNav.jsx, Admin\VipController.php (quita `index`/`countVipHealth`, mantiene `analysis`/`storeNote`/`whatsapp`), routes/web.php (quita `vips.index`), Admin/Vips/Index.jsx (eliminado), User.php (quita `scopeNeedsRenewal`), VipLoyaltyService.php (quita health/constantes, mantiene `sanitizeNoteBody`), HandleInertiaRequests.php (quita `vipRenewalAlertCount` de `adminStats`, no se usaba en frontend), MyReservations.jsx (limpia rama `from=vips`) |
| 2026-08-08 | Limpieza legacy pagos (Bizum/proof/CheckManager/adminStats) | Cursor | HECHO | mail sin Bizum/IBAN; rutas proof/taquilla huérfanas; CheckManager+adminStats; VipManager; props Bizum; PagoCuotaController/ProductoOferta/inspire |
| 2026-08-08 | Alquileres: pasarela+datáfono+efectivo (resto pendiente) + walk-in desde datáfono | Cursor | HECHO | migración `balance_status`/`balance_payment_method`/`balance_paid_at`, `Booking.php`, `PaymentGatewayService`, `DatafonoPaymentReconciliationService`, `BookingService`, `Admin\BookingController`, `Admin\DatafonoPaymentController`, `Admin/Bookings/Index.jsx`, `Admin/Payments/Datafono/Index.jsx`, `MyReservationsController` (limpieza), `routes/web.php` |
| 2026-08-08 | Pagos: retirar Dashboard Global (obsoleto) | Cursor | HECHO | GlobalNav, delete GlobalDashboard.jsx + PaymentValidationController, redirect `admin.payments.global` → datáfono |
| 2026-08-08 | Datáfono: ocultar Ignorar en Efectivo + label "Pendiente de asignar" | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` (STATUS_LABELS, filtro, botón Ignorar condicionado a source=tpv) |
| 2026-08-08 | Hub: Pagos → Gestor de servicios (menú Gestión) | Cursor | HECHO | CatalogOfferTabs, Catalog/Index, GlobalNav, breadcrumbs Bonos/Fotos/Taquillas |
| 2026-08-08 | Datáfono: label Referencia → Nombre del cliente (efectivo) | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` (columna Cliente, form efectivo, prefill Asignar) |
| 2026-08-08 | Pedidos: quitar UI «Pagado» (solo entrega operativa) | Cursor | HECHO | PedidoDetailModal, Pedido(s).jsx, PedidoController maps, Factory, SandboxSeeder |
| 2026-08-08 | Bonos: quitar «Asignar Bono Manual» (redundante con flujo Pagos/datáfono) | Cursor | HECHO | Admin/Bonos/Index.jsx, Admin\BonoController.php, routes/web.php |
| 2026-08-08 | Pagos: quitar página aislada «Validación de Bonos» (bonos siempre por pasarela/webhook; Dashboard Global mantiene fallback) | Cursor | HECHO | GlobalNav.jsx, Admin\PaymentValidationController.php, routes/web.php, Admin/Payments/Dashboard.jsx (eliminado) |
| 2026-08-08 | Fotos: precio base + plus×personas (pack) | Cursor | HECHO | plus_por_persona_cents, quotePriceCents, Admin/Photos, Servicios_Fotos, datáfono party_size, PhotoBookingPackPriceTest |
| 2026-08-08 | Gestor pedidos: confirmar al desmarcar Entregado | Cursor | HECHO | `GestorPedidos.jsx` (modal confirm), `PedidoDetailModal` |
| 2026-08-08 | Modal detalle pedido: UI oscura alineada al admin | Cursor | HECHO | `PedidoDetailModal.jsx` |
| 2026-08-08 | Datáfono inbound 1/3: source + registerRawPayment | Cursor | HECHO | source/raw_payload, unique ref, datafono2 off, registerRawPayment idempotente, tests 2/2 + suite 16/16 |
| 2026-08-08 | Datáfono UX: guest-first + auto-abrir Asignar | Cursor | HECHO | `Index.jsx`, `DatafonoPaymentController::store` (+flash) |
| 2026-08-08 | Kit UI admin (Fase 4c/4): Datáfono modal Asignar | Cursor | HECHO | `Index.jsx` (AdminFormField/AdminButton en modal Asignar) |
| 2026-08-08 | Datáfono inbound 3/3: UI (badge origen + botón efectivo + 1 terminal) | Cursor | HECHO | `Index.jsx` (columna Origen, botón "+ Cobro en efectivo", terminal oculto si hay 1 solo), `listPayments()` (+source) |
| 2026-08-08 | Datáfono inbound 2/3: DTO + endpoint firmado | Cursor | HECHO | `config/services.php` (bloque `datafono`), `.env.example`, `TpvPaymentIngestDto`, `DatafonoPaymentReconciliationService::ingestTpvPayment`, `DatafonoIngestWebhookController`, ruta `POST /webhooks/datafono/ingest` (CSRF except en `bootstrap/app.php`), `tests/Feature/Payments/DatafonoIngestWebhookTest.php` (6 tests). Suite `--filter=Datafono` 22/22 verde. |
| 2026-08-08 | Gestor pedidos: filas densas + WA icon + solo entrega | Cursor | HECHO | `GestorPedidos.jsx`, `PedidoDetailModal.jsx`, `PedidoController` (paginate 20) |
| 2026-08-08 | Gestor pedidos: solo entrega (sin toggle pagado) | Cursor | HECHO | `GestorPedidos.jsx`, `PedidoDetailModal.jsx`, `PedidoController`, ruta togglePagado |
| 2026-08-08 | Menú: Planes solo vía Gestor de pagos; hub «Pagos» | Cursor | HECHO | `GlobalNav.jsx`, Catalog Index/Tabs, breadcrumbs Bonos/Photos |
| 2026-08-08 | Datáfono: cliente no registrado (guest) en puntuales | Cursor | HECHO | pedidos guest_*, reconcile producto/fotos, listPayments, Index.jsx Asignar, DatafonoGuestClientTest 3/3 |
| 2026-08-08 | Esquema: rojo si cuota vencida con días de deuda | Cursor | HECHO | `LockerOccupantDto`, `buildLockerOccupancyMap`, `LockerOccupancyMap.jsx` |
| 2026-08-08 | Separar Esquema taquillas vs Planes taquillas | Cursor | HECHO | rutas, controller, Esquema.jsx, PlanesTaquillasAdmin, GlobalNav, CatalogTabs |
| 2026-08-08 | Unificar UI admin Catálogo — Fase 4b/4: Datáfono modal registrar | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` (modal Registrar → AdminFormField/AdminButton; Asignar intacto) |
| 2026-08-08 | Unificar UI admin Catálogo — Fase 4a/4: Datáfono toast+tabla | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` (toast + tabla principal migrados, modales intactos) |
| 2026-08-08 | Mapa ocupación: sticky + contactos + celdas verde/User 3D | Cursor | HECHO | `LockerOccupantDto`, `buildLockerOccupancyMap`, `LockerOccupancyMap.jsx` |
| 2026-08-08 | Mapa ocupación: panel fijo + ocupado granate | Cursor | HECHO | `LockerOccupancyMap.jsx` |
| 2026-08-08 | Unificar UI admin Catálogo — Fase 3/4: Fotos | Cursor | HECHO | `Admin/Photos/Index.jsx` migrado al kit (AdminTable sesiones+reservas) |
| 2026-08-08 | Taquillas: quitar Reasignar + mapa ocupación simple (hover) | Cursor | HECHO | `Registry.jsx`, `PlanesTaquillasAdmin.jsx`, `LockerOccupancyMap.jsx`, DTOs ocupación, service |
| 2026-08-08 | Unificar UI admin Catálogo — Fase 2/4: AdminTable + Bonos | Cursor | HECHO | `components/admin/ui/AdminTable.jsx`, `Admin/Bonos/Index.jsx` migrado a oscuro |
| 2026-08-08 | Unificar UI admin Catálogo — Fase 1/4: kit + Taquillas | Cursor | HECHO | `components/admin/ui/{AdminPageShell,AdminCard,AdminStatusBadge,AdminButton,AdminFormField,AdminToast}.jsx`, `PlanesTaquillasAdmin.jsx` |
| 2026-08-07 | Hub admin Catálogo / Ofertas (tabs + CRUDs existentes) | Cursor | HECHO | `CatalogHubController`, `CatalogOfferTabs.jsx`, Admin/Catalog/*, GlobalNav, 4 CRUDs |
| 2026-08-08 | Datáfono crear taquilla/bono nuevos (Tanda 3b) | Cursor | HECHO | reconcile plan/pack, catálogo Inertia, modal, DatafonoCreateTaquillaBonoTest 3/3 |
| 2026-08-07 | Fotos caducidad + estados (Tanda 3a) | Cursor | HECHO | expires_at, photos:cancel-expired, Confirm harden, tests 4/4 |
| 2026-08-07 | Datáfono stock + TicketBAI (Tanda 2b) | Cursor | HECHO | stock producto + PaymentConfirmed si no TicketBAI propio; tests 4/4 |
| 2026-08-07 | Fotos precio pack + validaciones públicas (Tanda 2a) | Cursor | HECHO | pack fijo, validaciones guest/fecha, UX errores, PhotoBookingPackPriceTest |
| 2026-08-07 | UX modal datáfono (Tanda 1b) | Cursor | HECHO | `Admin/Payments/Datafono/Index.jsx` |
| 2026-08-07 | Hardening datáfono Tanda 1 — ENUM + guards reconcile | Cursor | HECHO | ENUM `datafono`, guards reconcile, `DatafonoReconciliationGuardsTest` (4 passed) |
| 2026-08-07 | Modal alerta rojo cuota/taquilla al bloquear carrito | Cursor | HECHO | `VerificarTaquilla.php`, `HandleInertiaRequests.php`, `FlashErrorModal.jsx`, `PublicLayout.jsx` |
| 2026-08-07 | Dominio Fotos + panel conciliación datáfono | Cursor | HECHO | migraciones Photos/Datafono, Services, Controllers, Pages Admin/Photos y Admin/Payments/Datafono |
| 2026-08-07 | Fix icono carrito (redirige a /carrito) | Cursor | HECHO | `resources/js/components/GlobalNav.jsx` |
| 2026-08-06 | Modal unificado recibo + descarga en Vigencia admin | Cursor | HECHO | `Vigencia.jsx`, `Registry.jsx`, `PlanesTaquillasController.php`, `PaymentReceiptController.php` |
| 2026-08-06 | Distinguir "sin plan" (nunca pagó) de "vencido" en vigencia | Cursor | HECHO | `TaquillaMembershipService.php`, `Vigencia.jsx` |
| 2026-08-06 | Fase 3 — labels estado pago admin (Pagado/Por revisar/No válido) | Cursor | HECHO | `resources/js/Pages/Admin/Taquillas/Vigencia.jsx` |
| 2026-08-06 | Demo sandbox recibo Stripe (sin API real) | Cursor | HECHO | `SandboxRandomDemoSeeder.php`, `public/demo/recibo-stripe-sandbox.html` |
| 2026-08-06 | Fase 1 — unificar recibos Stripe en historial admin taquilla | Cursor | HECHO | `TaquillaMembershipService.php`, `Vigencia.jsx` |
| 2026-08-06 | Auditoría Fase 0 — recibos/justificantes taquilla | Cursor | HECHO (solo lectura) | ninguno (auditoría) |
| 2026-08-06 | Fix semáforo rojo con días restantes positivos (vigencia) | Cursor | HECHO | `TaquillaMembershipService.php`, `Vigencia.jsx` |
| 2026-08-06 | Refinar UI admin Vigencia taquillas (color/densidad) | Cursor | HECHO | `resources/js/Pages/Admin/Taquillas/Vigencia.jsx` |
| 2026-08-09 | Auditoría tests + riesgos pagos/datáfono/alquileres | Cursor | HECHO | Suite: 197 OK / 1 FAIL (`PasswordUpdateTest`); Datafono 34/34; Rentals/Taquilla/Photos/Mostrador OK. Bug real: `PasswordController` llama `logoutOtherDevices` tras cambiar hash |
| 2026-08-09 | Pagos: Stripe abandonado = deshacer; sin Bizum/transferencia en vigencia | Cursor | HECHO | purge pending taquilla (+legado sin expires_at); label «Pendiente de pago»; `rentals:expire-pending-unpaid` schedule; Vigencia method labels |
| 2026-08-09 | Datáfono ticket: rediseño modal (header/footer sticky + AdminCard) | Cursor | HECHO | `MostradorTicketModal.jsx` (solo maquetación/estilos, sin tocar lógica) |
| 2026-08-09 | Vigencia: icono recibo bajo Pagado + sort móvil = escritorio | Cursor | HECHO | Recibo bajo badge; MOBILE_SORT = columnas Progreso/Baja/Usuario/Taquilla ± dirs |
| 2026-08-09 | Vigencia: barra solo por días (restantes o atraso), no por duración plan | Cursor | HECHO | `DAYS_BAR_SCALE=90` para activos y vencidos; ignorar duración plan |
| 2026-08-09 | Datáfono ticket: quitar «Forzar importe distinto» (cobro=ticket siempre) | Cursor | HECHO | MostradorTicketModal, DatafonoPaymentController, MostradorTicketService, DatafonoPaymentReconciliationService |
## Última actividad (archivada)
- **2026-08-09** — Cursor: Datáfono ticket Alquiler UX — Modo en pastillas Horas/Días; packs como chips con precio € (solo `prices > 0`); sync pack al cambiar tabla/modo; botón Añadir deshabilitado solo en alquiler incompleto. HECHO.
- **2026-08-08** — Cursor: Datáfono ticket Clase — catálogo de clases próximas (filtro tipo/modalidad), reserva walk-in al añadir línea (`lesson_id` → LessonUser confirmado datafono); sigue pudiendo enlazar pendientes. HECHO.
- **2026-08-08** — Cursor: Datáfono detalle — `listPayments` resume cada línea (plan/pack/tabla/clase/fotos/productos + fechas de servicio); acordeón móvil muestra Cobrado + Servicios; desktop columna Dominio → Servicios. HECHO.
- **2026-08-08** — Cursor: Datáfono móvil reaplicado sobre ticket multi-servicio (el rewrite había pisado el acordeón) + `npm run build` (sin `public/hot`). Cards <md / tabla ≥md. HECHO.
- **2026-08-08** — Cursor: Datáfono móvil — lista en cards acordeón (`md:hidden`): fila compacta (fecha · importe · origen · estado + chevron); detalle (cliente, dominio, terminal, acciones) al expandir. Tabla desktop intacta (`≥md`). Sin scroll horizontal. HECHO.
- **2026-08-08** — Cursor: Segunda mano — el «eliminar» pasa a soft-delete (`deleted_at`): no borra historial ni fotos; sale del catálogo público; filtro «Desactivadas» + botón Reactivar (`admin.second-hand.restore`). HECHO.
- **2026-08-08** — Cursor: limpieza legacy pagos — mail clase sin Bizum/IBAN; eliminados upload-proof/subir-justificante y rutas admin taquilla huérfanas; CheckManager + `adminStats`; VipManager.jsx; props Bizum en front; PagoCuotaController/ProductoOferta/`inspire`. Commander reenganchado desde ClassManager («Consola del día»). HECHO.
- **2026-08-08** — Cursor: Alquileres actualizados a la nueva lógica de pagos (pasarela+datáfono+efectivo, sin residuo del flujo antiguo de comprobante manual). Motor de inventario/disponibilidad/precios (`BookingService`, `Surfboard`, `PriceSchema`) intacto. (1) **Modelo**: migración `balance_status` (none/pending/confirmed) + `balance_payment_method` + `balance_paid_at` en `bookings`; helpers `Booking::remainingBalanceCents()`/`hasBalanceDue()`; `PaymentGatewayService::confirmBookingPayment` fija `balance_status=pending` si el depósito Stripe no cubre el total. (2) **Conciliar el resto**: `DatafonoPaymentReconciliationService::pendingCandidatesForUser('alquiler')` ahora también lista bookings con depósito ya confirmado y resto pendiente (`amount_cents=remainingBalanceCents()`); `confirmBooking()` bifurca depósito vs. resto (resto solo actualiza `balance_status=confirmed`/`balance_payment_method`/`balance_paid_at`, sin reabrir `payment_status`); nuevo `chargeBookingBalanceCash()` para cobro rápido en efectivo. (3) **Walk-in desde datáfono**: nuevo caso en `reconcile()` para categoría `alquiler` sin `payable_id` (`resolveAlquilerPayable`, reutiliza `checkAvailability`/pricing existente), crea el Booking ya `confirmed`/`balance_status=none` cobrando el 100% en el momento; `Admin/Payments/Datafono/Index.jsx` con selector de tabla + modo hora/día + pack + fecha de recogida. (4) **UI admin reservas**: `Admin/Bookings/Index.jsx` muestra depósito/resto con badges (Pendiente/Cobrado) + botón "Cobrar resto (efectivo)"; `canMarkPickup`/`BookingService::markPickedUp` bloquean la entrega de la tabla si `hasBalanceDue()`. (5) **Limpieza**: eliminado `MyReservationsController::uploadRentalProof` (stub) + su ruta; quitada la opción "Completada" del filtro de estado (inalcanzable, se documenta en `Booking::STATUS_COMPLETED`); `Admin\BookingController::store()` documentado como reserva manual por teléfono sin cobro inmediato (walk-in ya pagado vive en datáfono). Tests: `RentalHardeningCierreTest` (+2 casos bloqueo/desbloqueo de recogida), `DatafonoReconciliationGuardsTest` (+resto/candidatos/walk-in), nuevo `PaymentGatewayBookingBalanceTest`; suite Rentals+Datafono+Payments 100% verde (único fallo en la suite general es `PasswordUpdateTest`, no relacionado). `npm run build` OK. HECHO.
- **2026-08-08** — Cursor: retirado **Dashboard Global** (obsoleto frente a Stripe+datáfono): fuera del menú; página y `PaymentValidationController` eliminados; `admin.payments.global` redirige a datáfono; featured Pagos = datáfono. HECHO.
- **2026-08-08** — Cursor: Datáfono **ticket multi-servicio** — un cobro (efectivo o TPV) puede llevar N líneas (producto/fotos/alquiler/clase/bono/taquilla según perfil). Tablas `mostrador_tickets` + `mostrador_ticket_lines` (backfill 1:1); `MostradorTicketService::closeCashTicket` / `assignTpvTicket`; reconcile legacy crea ticket de 1 línea; UI `MostradorTicketModal` (efectivo = ticket directo; TPV = asignar con suma==importe). Tests `MostradorTicketMultiLineTest` 5/5; suite Datafono 29/29; build OK. HECHO.
- **2026-08-08** — Cursor: Datáfono — botón "Ignorar" ahora solo visible en filas `source=tpv` (en `Efectivo` ya no aporta nada porque siempre se asigna a socio o "Cliente no registrado" al crear el cobro); etiqueta de estado `pending_review` renombrada de "Por revisar" a "Pendiente de asignar" (badge, filtro desplegable y copy del modal de efectivo) para no confundir con "verificar datos". Cero cambios de backend (`reconcile`/`ignore`/estados intactos); suite Datafono 22/22; `npm run build` OK. HECHO.
- **2026-08-08** — Cursor: hub admin renombrado a **Gestor de servicios** (ya no «Pagos»); menú Gestión → grupo Servicios; Pagos solo cobros (dashboard/clientes/datáfono). HECHO.
- **2026-08-08** — Cursor: Datáfono efectivo — «Referencia» → «Nombre del cliente» (form + columna); al Asignar, prefill guest con ese nombre. HECHO.
- **2026-08-08** — Cursor: quitado badge/estado UI «Pagado» en modal admin + listado/detalle cliente; payload Inertia sin `pagado`/`payment_status`; factory/seeder demo solo pedidos cobrados. Columna BD `pagado` se mantiene (pasarela Stripe). HECHO.
- **2026-08-08** — Cursor: Fotos packs = precio base + (N × plus/persona); migración `plus_por_persona_cents`; admin/público/datáfono con `party_size` y total esperado; reconcile valida vs catálogo; tests pack actualizados. HECHO.
- **2026-08-08** — Cursor: confirmación profesional al desmarcar Entregado (tarjeta + modal detalle); marcar entregado sin confirm. HECHO.
- **2026-08-08** — Cursor: `PedidoDetailModal` pasado a tema oscuro admin (slate/cyan, borde entrega rojo/verde, WA compacto, pills estado). HECHO.
- **2026-08-08** — Cursor: Gestor de pedidos — listado en **una fila** (# · nombre · tel · `WhatsAppIcon` · toggle Entregado · Ver); stats/chips compactos; 20/página; reutiliza icono WA del resto del admin. HECHO.
- **2026-08-08** — Cursor: Datáfono UX — (1) modal Asignar: toggle "Socio registrado/Cliente no registrado" ahora es lo primero que se ve (antes que la categoría); al elegir "no registrado" las pastillas de categoría se filtran a Producto/Fotos (únicas compatibles) con nota explicativa. (2) "Fecha cobro" del registro en efectivo se precarga con la hora actual (editable). (3) al guardar un cobro en efectivo se abre automáticamente el modal Asignar para esa fila (`store()` flashea `datafono_created_payment`, frontend lo consume vía `useEffect` sobre `flash`). Cero cambios en `reconcile`/`ignore`/validaciones; suite Datafono 22/22. HECHO.
- **2026-08-08** — Cursor: Kit UI admin (Fase 4c/4) — modal "Asignar" de Datáfono migrado a `AdminFormField`/`AdminButton` (nombre/email invitado, filtro productos, selects sesión/pendiente/plan/pack, textarea notas, botones Cancelar/Conciliar). Sin tocar `Combobox` de socio, pastillas de categoría/toggle ni checklist de productos (riesgo/beneficio no compensaba). Cero cambios de lógica; suite Datafono 22/22. Con esto se cierra el kit UI admin unificado (4/4 completo). HECHO.
- **2026-08-08** — Cursor: Datáfono inbound 3/3 — `listPayments()` añade `source`; `Index.jsx`: columna "Origen" (badge TPV=verde / Efectivo=ámbar), botón renombrado "+ Cobro en efectivo" (antes "+ Registrar cobro") + copy aclaratorio, selector de terminal oculto (registro y filtro) si solo hay 1 activo. `reconcile`/`assign`/`ignore` sin tocar; suite Datafono 22/22. HECHO.
- **2026-08-08** — Cursor: Datáfono inbound 2/3 — `TpvPaymentIngestDto`, `DatafonoPaymentReconciliationService::ingestTpvPayment` (resuelve terminal por `terminal_codigo`/default, delega en `registerRawPayment`), `DatafonoIngestWebhookController` (firma HMAC-SHA256 propia, 401/422/503), ruta pública `POST /webhooks/datafono/ingest` (CSRF except), config `services.datafono`; tests nuevos 6/6; suite Datafono 22/22. UI/controller admin sin tocar. HECHO.
- **2026-08-08** — Cursor: Datáfono inbound 1/3 — migración `source`/`raw_payload` + unique `external_reference`; `datafono2` inactivo; `registerRawPayment` con source (default `manual_cash`) e idempotencia TPV; tests nuevos 2/2; suite Datafono 16/16. UI/webhook sin tocar. HECHO.
- **2026-08-08** — Cursor: Gestor de pedidos — tarjeta mínima (#, nombre, tel+WA, Entregado, Ver pedido); quitado toggle/filtros/stats de pago; listado solo `pagado=true`; eliminada ruta `pedido.togglePagado`. HECHO.
- **2026-08-08** — Cursor: menú Gestión — quitado «Planes taquillas» del grupo Taquillas (acceso solo desde hub); grupo Catálogo eliminado; en **Pagos** el featured es **Gestor de pagos** (`admin.catalog.index`); hub H1/título «Pagos». HECHO.
- **2026-08-08** — Cursor: Esquema taquillas — celdas rojo granate suave si cuota vencida con `dias_deuda > 0` (solo taquilla; panel muestra días). HECHO.
- **2026-08-08** — Cursor: datáfono «cliente no registrado» (Producto/Fotos): guest en pedidos + fotos; listPayments; candidatos con `guest_name`; hint en modal Asignar; `DatafonoGuestClientTest` 3/3. HECHO.
- **2026-08-08** — Cursor: Unificar UI admin Catálogo — Fase 4b/4. Modal «Registrar cobro» → `AdminFormField` (Terminal/Importe/Fecha/Referencia) + `AdminButton` (Cancelar/Guardar). Modal «Asignar» verificado byte-a-byte intacto (hash SHA256 coincidente). `npm run build` OK. HECHO.
- **2026-08-08** — Cursor: separado **Esquema taquillas** (`/taquilla/admin/esquema`, mapa) y **Planes taquillas** (`/taquilla/admin/index`, CRUD planes). Menú Gestión actualizado. HECHO.
- **2026-08-08** — Cursor: mapa ocupación — panel solo por click (sin abrir/cerrar al hover); WA solo icono; título página «Ocupación de taquillas» (planes de cuota abajo). HECHO.
- **2026-08-08** — Cursor: mapa ocupación — DTO con email/telefono; panel sticky con mailto + WhatsApp (`whatsappUrlFromPhone`); celdas verdes 3D (ocupada = icono User, sin rojo); hover previsualiza, click fija. HECHO.
- **2026-08-08** — Cursor: mapa ocupación — panel detalle siempre visible («Selecciona una taquilla» si vacío); ocupadas en granate suave; click fija selección (el hover ya no abre/cierra el div). `LockerOccupancyMap.jsx`. HECHO.
- **2026-08-08** — Cursor: eliminada pestaña «Reasignar» del Registro de pagos; mapa simple de ocupación en Planes (`LockerOccupancyMap`, hover/tap → nombre y apellido). DTOs `LockerOccupantDto` / `LockerOccupancyMapDto` + `buildLockerOccupancyMap()`. HECHO.
- **2026-08-08** — Cursor: Unificar UI admin Catálogo — Fase 4a/4. `Admin/Payments/Datafono/Index.jsx`: toast de flash → `AdminToast`; tabla principal de cobros → `AdminTable` con `AdminStatusBadge` (pending_review=warning, assigned=success, ignored=neutral) y botones Asignar/Ignorar → `AdminButton`. Modales "Registrar cobro" y "Asignar" NO tocados (quedan para 4b/4c). `npm run build` OK. HECHO.
- **2026-08-08** — Cursor: Unificar UI admin Catálogo — Fase 3/4. `Admin/Photos/Index.jsx` migrado a `AdminPageShell`/`AdminCard`/`AdminTable` (sesiones y reservas)/`AdminStatusBadge`/`AdminButton`/`AdminFormField`/`AdminToast`; botón primario `fuchsia-600`→`cyan-600` alineado con el resto; mismos `route()`/payloads. `npm run build` OK. Datáfono sin tocar (Fase 4 pendiente). HECHO.
- **2026-08-08** — Cursor: Unificar UI admin Catálogo — Fase 2/4. Nuevo `AdminTable.jsx` (genérico, columns/rows/renderCell). `Admin/Bonos/Index.jsx` migrado de tema claro/sin Layout a `AdminPageShell` oscuro + `AdminTable`/`AdminCard`/`AdminButton`/`AdminFormField`/`AdminStatusBadge`/`AdminToast`; mismos `route()`/payloads. `npm run build` OK. Fotos/Datáfono sin tocar (fases 3-4 pendientes). HECHO.
- **2026-08-08** — Cursor: Unificar UI admin Catálogo — Fase 1/4. Kit `components/admin/ui/*` (AdminPageShell, AdminCard, AdminStatusBadge, AdminButton, AdminFormField, AdminToast) extraído del patrón oscuro ya usado en Taquillas; `PlanesTaquillasAdmin.jsx` migrado sin cambios de comportamiento (mismos `route()`/payloads). `npm run build` OK. Bonos/Fotos/Datáfono sin tocar (fases 2-4 pendientes). HECHO.
- **2026-08-08** — Cursor: Datáfono Tanda 3b — crear taquilla/bono desde catálogo (`plan_taquilla_id` / `pack_bono_id`) vía `TaquillaMembershipService::createPendingPaymentForCheckout` + `BonoService::requestBono`/`confirmBono`; props catálogo en index; modal pendientes + «Crear nuevo»; tests 3/3. HECHO.
- **2026-08-07** — Cursor: hub admin **Catálogo / Ofertas** (`/admin/catalogo`): tabs compartidos a Taquillas / Bonos VIP / Clases / Fotos + placeholder Surfskate; menú Gestión → Catálogo; CRUDs de dominio intactos. HECHO.
- **2026-08-07** — Cursor: Fotos Tanda 3a — `expires_at` + `photos:cancel-expired` cada 5 min; confirm bloquea cancelled/rejected/caducadas; admin `can_confirm`/`is_expired`. Tests 4/4. HECHO.
- **2026-08-07** — Cursor: Datáfono Tanda 2b — `createPaidPedido` con stock/precios catálogo; si `emite_ticketbai_propio=false` → `PaymentConfirmed` (B2B); skip recibo Stripe si session no `cs_`. Tests 4/4. HECHO.
- **2026-08-07** — Cursor: Fotos Tanda 2a — precio pack fijo (no × party_size), `fecha_inicio after:now`, guest nombre+email required, errores en `Servicios_Fotos`, tests 2/2. HECHO.
- **2026-08-07** — Cursor: UX modal datáfono — flash/errors visibles, modal no cierra en error, labels ES, force mismatch + notas, confirm Ignorar, columna Dominio. HECHO.
- **2026-08-07** — Cursor: lápiz «Editar mi cuenta» del menú móvil pegado al nombre (no a la derecha del todo); «Salir» sigue a la derecha. `GlobalNav.jsx`. HECHO.
- **2026-08-07** — Cursor: Hardening datáfono Tanda 1 — ENUM `datafono` en bookings/lesson_user; guards (pending, anti-doble vínculo, match importes + force, ownership); tests 4/4. HECHO.
- **2026-08-07** — Cursor: dominio **Fotos** (`photo_sessions` / `photo_session_bookings`, `PhotoBookingService`, Stripe payable, admin CRUD, fiscal/historial) + panel **Pagos datáfono** (`payment_terminals` / `datafono_payments`, conciliación a 6 categorías, TicketBAI propio por terminal). Nav Pagos actualizado. HECHO.
- **2026-08-07** — Cursor: modal rojo bloqueante al intentar usar el carrito sin taquilla o con cuota vencida. `VerificarTaquilla` flash `access_alert` → `FlashErrorModal` en `PublicLayout` (hay que pulsar «Entendido»/X; no se cierra al tocar el fondo). HECHO.
- **2026-08-07** — Cursor: fix icono carrito en `GlobalNav.jsx`. `safeRoute` ahora genera URLs relativas (evita que Ziggy apunte al `APP_URL`/túnel distinto del host actual); el icono apunta a `/carrito` con fallback explícito. HECHO.
- **2026-08-06** — Cursor: modal unificado de recibo/justificante en Vigencia (y Registry). Siempre abre modal: si es Stripe → mensaje + «Abrir recibo» (pestaña nueva, no iframe); si es justificante local → preview + «Descargar» (`?download=1` en `showProof` / recibos con `storage_path`) + Cerrar. HECHO.
- **2026-08-06** — Cursor: los ~50 socios demo de `TaquillaDemoCoherentSeeder` (`demo.taquilla{N}@s4.test`) ahora generan comprobante para cada pago confirmado (antes salía "—" en la columna Recibo del admin, ver captura del usuario). "card" → `PaymentReceipt` fake apuntando a `public/demo/recibo-stripe-sandbox.html` (abre en pestaña nueva); "bizum"/transferencia → `payment_proof_path` real en `storage/app/private/taquilla-proofs/demo-justificante.html` (se abre embebido en el modal). Backfill puntual aplicado a los 69 pagos confirmados ya existentes (0 sin comprobante tras el backfill). Archivos: `database/seeders/TaquillaDemoCoherentSeeder.php`, `storage/app/private/taquilla-proofs/demo-justificante.html` (nuevo). Solo afecta a datos sandbox (emails `@s4.test`), no toca lógica de producción. HECHO.
- **2026-08-06** — Cursor: añadidos tests feature `tests/Feature/Taquilla/VigenciaPayloadTest.php` (7 casos: activo, vencido, activo por pago futuro, sin plan, pago pendiente/rechazado no cuenta, prepago tras vencido, exclusión sin taquilla). Suite completa: 142 passed. HECHO.
- **2026-08-06** — Cursor: `buildVigenciaPayload()` distingue ahora `estado: 'sin plan'` (socio con taquilla asignada pero cero pagos confirmados nunca) de `estado: 'vencido'` (tuvo cobertura y caducó). En `Vigencia.jsx`: `rowUrgency` trata ambos como urgentes (mismo rojo/borde), pero `daysLabel` ya muestra "Sin plan" en vez de "Vencido"; también se ajustó el mensaje de WhatsApp y el tooltip de aviso de baja para no decir "ha vencido el sin fecha" en estos casos. Verificado contra BD real: 6 socios afectados (ej. Cesar Dopico, taquilla #0), antes mal etiquetados como "Vencido". HECHO.
- **2026-08-06** — Cursor: Fase 3 — `paymentStatusLabel` en `Vigencia.jsx` (admin) ahora dice "Pagado / Por revisar / No válido" en vez de "Confirmado / Pendiente / Rechazado"; `paymentMethodLabel` simplificado para no duplicar/contradecir el estado (siempre muestra el método real, no un status). Cliente (`PlanesTaquillasClient.jsx`) no se toca: ya usa etiquetas contextuales propias (En vigor/Preparado/Finalizado). HECHO.
- **2026-08-06** — Cursor: añadido pago demo (tarjeta) + `PaymentReceipt` simulado en `SandboxRandomDemoSeeder` para el cliente #4 (`$clients[3]`), con `receipt_url` apuntando a página estática `public/demo/recibo-stripe-sandbox.html`. Permite probar "Ver recibo" (abre en pestaña nueva) sin credenciales reales de Stripe. Requiere re-ejecutar `php artisan db:seed --class=SandboxRandomDemoSeeder`. HECHO.
- **2026-08-06** — Cursor: Fase 1 recibos taquilla — `userPaymentHistory()` ahora mezcla recibo Stripe (`PaymentReceiptAccessService`) + justificante manual, igual que `buildClientIndex()`. En `Vigencia.jsx` (admin), "Ver recibo/Ver" abre el recibo Stripe en pestaña nueva (bloquea iframe por `X-Frame-Options`) y el justificante manual sigue en el modal embebido. HECHO.
- **2026-08-06** — Cursor: auditoría Fase 0 de recibos/justificantes de taquilla (sin cambios de código). Gap encontrado: `TaquillaMembershipService::userPaymentHistory()` (usado por admin en `Vigencia.jsx`) no mezcla recibos Stripe vía `PaymentReceiptAccessService`, a diferencia de `buildClientIndex()` y `enrichRegistryRowsWithReceipts()` que sí lo hacen. Propuesta: Fase 1 = unificar ese método. Ver informe completo en el chat.
- **2026-08-06** — Cursor: fix vigencia — no pintar rojo si `dias_restantes > 0`; backend ya no marca `vencido` cuando el fin del periodo (o prepago futuro) sigue vigente. HECHO.
- **2026-08-06** — Cursor: refinamiento UI `Vigencia.jsx` (semáforo solo en urgentes, barras suaves, WhatsApp outline, densidad py-2, badges muted). HECHO.
- **2026-08-04** — Reasonix: creado `docs/COMPETENCIA_SEO_DONOSTIA.md` (análisis competitivo de escuelas de surf de Donostia + oportunidades SEO + pendientes: análisis de keywords de la competencia y plan de rebrand/SEO). No toca código de la app. PENDIENTE para el dueño (recordatorio a 3 días: 2026-08-07).
- **2026-06-18** — Reasonix: creada infraestructura del taller de prompts (`docs/taller-prompts/PROTOCOLO.md`, `REGISTRO.md`, `COORDINACION.md`) y actualizado `docs/PROJECT_TREE_FOR_GEMINI.md`. HECHO.
- **2026-08-03** — Reasonix: configurado túnel Cloudflare (named + quick), creado túnel `masquesurf` con credenciales en `~/.cloudflared/`, config.yml apuntando a `sansebastiansurfschool.eu`, CNAME enrutado, nameservers cambiados en DonDominio (pendiente propagación DNS). Revertido a modo desarrollo normal. HECHO.
- **2026-08-09** — Cursor: auditoría tests pagos/alquileres — suite completa 197 OK / 1 FAIL (`PasswordUpdateTest`: `PasswordController` hace `logoutOtherDevices` **después** de hashear la nueva clave → 500). Datafono 34/34, Rentals/Taquilla/Photos/Mostrador OK. HECHO.
- **2026-08-09** — Cursor: Stripe abandonado no deja cuota fantasma — `purgeExpiredPendingPayments` también borra pending sin `expires_at` (demo/legado); historial Vigencia oculta caducados; etiqueta «Pendiente de pago» (no «Por revisar»); schedule `rentals:expire-pending-unpaid`. Bizum/transferencia ya fuera del ENUM (migración previa). HECHO.
- **2026-08-09** — Cursor: Datáfono ticket, rediseño visual del modal (`MostradorTicketModal.jsx`) sin tocar lógica de negocio: (1) cabecera fija con título/subtítulo + botón cerrar (X); (2) cuerpo con scroll propio (`flex-1 overflow-y-auto`) para no perder el pie con formularios largos (alquiler/clase); (3) pie fijo (`shrink-0 border-t`) con el Total del ticket + aviso de descuadre TPV + botones Cancelar/Cobrar siempre visibles, sin depender de hacer scroll hasta el final; (4) secciones «Añadir línea» y «Ticket» ahora usan `AdminCard` (estándar del kit admin) en vez de `div` sueltos; (5) lista de líneas con separadores (`divide-y`) y botón «Quitar» alineado al patrón ya usado en el resto de la app (`text-rose-300/90 hover:text-rose-200`). Cero cambios en estados/handlers/validaciones/payload; compatible con la limpieza de «Forzar importe distinto» hecha en paralelo (ver entrada siguiente). `npx vite build` OK. HECHO.
- **2026-08-09** — Cursor: sugerencia de pendiente al conciliar TPV — si el importe restante (cobro TPV − líneas ya añadidas) coincide exacto con un pendiente del socio, se autoselecciona la primera vez y se marca `✓ … (coincide con el cobro)` en el desplegable; no se re-fuerza si el admin lo deselecciona manualmente (ref por carga de categoría). Confirmado además que "mostrar errores del backend sin cerrar el modal" ya estaba implementado (`ErrorList` + `errors` prop + solo `onSuccess` cierra). `MostradorTicketModal.jsx`. `npm run build` OK. HECHO.
- **2026-08-09** — Cursor: pulido tras quitar force mismatch — aviso TPV ahora dice importe exacto (`Faltan/Sobran X €`, no solo "no coincide"); campo Notas colapsado tras botón «+ Añadir nota» (ya no es obligatorio tras quitar el force, así que no hace falta mostrarlo siempre). `MostradorTicketModal.jsx`. `npm run build` OK. HECHO.
- **2026-08-09** — Cursor: quitado «Forzar importe distinto» del ticket mostrador (UI + backend). Cobro y líneas deben cuadrar siempre; el resto 70 % del alquiler sigue usando `remainingBalanceCents` sin escape hatch. Suite Datafono 34/34. HECHO.
- **2026-08-09** — Cursor: Datáfono ticket Clase — si no hay programadas, botón «Crear clase» (particular/grupal: inicio, duración, nivel, precio); crea Lesson y la selecciona en el ticket. Semanal → Commander. HECHO.
